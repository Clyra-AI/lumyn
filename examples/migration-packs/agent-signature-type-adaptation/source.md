# Synthetic signature and type adaptation

The provider changes user retrieval from a string identifier to a structured
lookup. The provider evidence does not define the consumer's adapter API: the
consumer must coordinate the caller and SDK-facing wrapper while preserving
list behavior. This source is authored for the Lumyn benchmark and is not
provider or customer evidence.
